[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[processes]
web = "python -m uvicorn main:app --host 0.0.0.0 --port $PORT --proxy-headers"

[variables]
PYTHONUNBUFFERED = "1"
